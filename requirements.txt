# VF Curve Manager Tool v2.2 - Requirements
# ============================================================
# NOTE: All processing is 100% local — no network calls, no
# telemetry, no data exfiltration.  Hardware data never leaves
# the machine.
# ============================================================

# GUI Framework (UI mode only — optional; CLI works without it)
PyQt5>=5.15.0

# Data processing & export
pandas>=1.3.0
numpy>=1.21.0
openpyxl>=3.0.9
scipy>=1.7.0

# Plotting (saved to local PNG files — no display required)
matplotlib>=3.4.0

# Table formatting (CLI output)
tabulate>=0.8.9

# Logging colours
colorama>=0.4.4

# Process management (optional: falls back to powershell/taskkill if absent)
psutil>=5.9.0

# Testing (mock mode; no hardware required)
pytest>=7.4.0
pytest-cov>=4.0.0
pytest-qt>=4.2.0

# Intel Hardware Access (pre-installed in Intel environment — no pip install)
# pysvtools
# itpii
# ipccli
# namednodes
